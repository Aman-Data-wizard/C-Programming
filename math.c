#include <stdio.h>
#include <math.h>
#include <ctype.h>

int main(){

   int a = 5;
   int b =2;
   
   pow(a,b);
   double result = pow(a,b);
   printf("Power: %lf", result);
     return 0;
}